# ── Base de datos SQLite ──────────────────────────────────────────────────────
# En Docker: usa la ruta del volumen montado en /app/data
DB_PATH=/app/data/data.db

# En desarrollo local (sin Docker): descomenta esta línea y comentá la de arriba
# DB_PATH=./data.db
